CREATE TABLE wenance_challenge (
       id_wenance_challenge         INTEGER IDENTITY PRIMARY KEY,
       curr1 VARCHAR(255),
       curr2 VARCHAR(255),
       `date`  DATETIME,
       lprice  DOUBLE
);
